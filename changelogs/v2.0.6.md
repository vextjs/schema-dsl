# v2.0.6 — Exact Dependency Metadata Patch (2026-06-09)

> Patch release for the v2.0 latest line. This release updates package dependency metadata without changing runtime behavior.

---

## Changes

- Pinned direct `dependencies` and `devDependencies` in `package.json` to exact versions resolved by the current lockfile.
- Kept package exports, runtime source and public API unchanged.
- Updated root changelog metadata for the patch release.

---

## Validation

- `npm ci`
- `npm run test:all:with-audit`
- `npm pack --dry-run --json`
