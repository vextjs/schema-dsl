# v1.1.5 变更摘要

本文件用于保留历史文档链接。当前 TypeScript 重构版的完整变更请以根目录 [CHANGELOG.md](../CHANGELOG.md) 为准。

## 升级指南

- 多语言错误对象格式继续兼容字符串格式。
- 建议通过 `Locale.addLocale()` 或 `dsl.config({ i18n })` 注册语言包。

## 最佳实践

- 错误代码使用稳定 key。
- 对前端展示使用 `error.toJSON()` 的结构化结果。

